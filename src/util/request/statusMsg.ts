import { formatMessage } from '../intl';

export default {
  500: formatMessage({
    id: 'request.500',
  }),
  404: formatMessage({
    id: 'odc.src.util.request.RequestException',
  }),
  405: formatMessage({
    id: 'odc.src.util.request.RequestException',
  }),
};
